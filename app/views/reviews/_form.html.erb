<%= simple_form_for [character, review] do |f| %>
  <%= f.input :content %>
  <%= f.input :reco, as: :radio_buttons,
  collection: [['Yes', true], ["No", false]], label: "I recommand ?" %>
  
  <%= f.submit "Submit", class: "btn-ghost btn-ghost:hover" %>
<% end %>
